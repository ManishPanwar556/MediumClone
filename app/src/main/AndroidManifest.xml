<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mediumclone2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.AndroidApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MediumClone"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediumClone2">
        <activity android:name=".ui.articlesFeed.ArticlesFeedActivity"></activity>
        <activity android:name=".ui.home.ArticlePostActivity">

        </activity>
        <activity android:name=".ui.home.ArticleActivity">

        </activity>
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>